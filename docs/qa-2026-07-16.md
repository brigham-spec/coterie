# Coterie — QA Sweep Report (2026-07-16)

## Scope & Method

Full MCP-driven QA sweep against the production build: non-AI paths plus AI
positive-path scenarios A–H. AI negative-path was verified **in code** rather
than by breaking the live app (unsetting `ANTHROPIC_API_KEY` on Vercel would
have taken down the running HVEDC pilot).

## Surface Results

| Area | Result | Notes |
|---|---|---|
| Non-AI CRUD (companies, contacts, projects, commitments, meetings, intros, events) | PASS | RLS-scoped reads/writes correct across the board |
| AI positive-path A–H (enrich-web, enrich-meetings, analyze-document, email extract, funding suggest, news scan) | PASS | Review-then-apply seams persist only operator-selected fields |
| AI negative-path (missing/invalid API key) | PASS (code-verified) | See below |
| Meeting logging (company profile) | FAIL → FIXED | Blocker below |
| News card / Project Press & News rendering | PARTIAL → FIXED | `<cite>` leak below |
| DB reliability under load | PARTIAL → FIXED | Intermittent 503s below |

## AI Negative-Path Verification (code)

`extractEmailThreadAction` and `enrichFromWebAction` are **ephemeral** (no DB
writes). Both map `Anthropic.AuthenticationError` → `"AI is not configured.
Check the API key."` and fall through to a generic catch otherwise. A
bad/absent key surfaces a clean message with zero partial writes. **PASS.**

## Bugs Found (7)

| # | Severity | Bug | Status |
|---|---|---|---|
| 1 | **BLOCKER** | Logging a meeting with zero attendees crashed the page — `logMeeting` was a throwing `Promise<void>`, so a raw `<form action>` submit (checkboxes can't be `required`) became an unhandled server-action error = full page crash | FIXED `c20f065` |
| 2 | **MAJOR** | `web_search` `<cite index="…">` markup leaked as literal tag text on the news card and into saved `NewsItem` rows (Project Press & News) | FIXED `c20f065` |
| 3 | **MAJOR** | Intermittent 503s on RSC / server-actions under prefetch concurrency — DB connection exhaustion (direct Neon endpoint, no pool tuning, no connection timeout) | FIXED `0cd7a22` + pooler env switch |
| 4 | Minor | Meeting date renders off-by-one between the Meetings **Card** and the **Timeline** (UTC vs local) | Deferred |
| 5 | Minor | New company/project gets **no initial Stage-History row** | Deferred |
| 6 | Minor | **Tier dropdown** stays stale until Settings is re-saved | Deferred |
| 7 | Minor | Commitments **"Scan" link** goes to the general Meetings list, not the specific meeting | Deferred |

## Fixes Applied

- **#1 Blocker** — `logMeeting` now returns
  `LogMeetingState = {status:"saved"} | {status:"error";message}` (single-arg,
  test-compatible, preserves auto-close). `MeetingForm` sets an inline error via
  `useState` inside the async form handler (event-driven, lint-clean) and
  renders `<p text-red-ink>`. 4 validation tests flipped from `.rejects.toThrow`
  to `expect(state).toEqual({status:"error",message})`; messages now
  sentence-case.
- **#2 Major** — `stripCitations(v) = v.replace(/<\/?cite\b[^>]*>/gi,"")`
  applied at parse time in `coerceArticle`'s `str()` helper, so it cleans
  **before** save. +1 test.
- **#3 Major** — Two-part: (a) `src/lib/prisma.ts` pool tuning (`max:5`,
  `connectionTimeoutMillis:5000`, `idleTimeoutMillis:10000`) — fail-fast instead
  of blocking into a 503; (b) prod `DATABASE_URL` switched to Neon's **pooled**
  endpoint (PgBouncer transaction-mode; `set_config(...,true)` GUC still works,
  RLS intact). `DIRECT_URL` stays direct for migrations. Prod alias health check
  → **200**.

## Data Cleanup

One org (HVEDC pilot, `f0000000-0000-4000-8000-000000000001`). Removed all QA
scratch rows: "QA Sweep Co", "QA Sweep Project", "QA Tester", plus 1
introduction that RESTRICT-referenced the QA contact. Final: 0 QA rows
remaining.

## Gate

`lint` / `typecheck` / `build` (25 routes) + **632 tests** (86 files) — all
green.

## Prioritized Remaining Work

1. Verify the 503 fix holds under real traffic (now shipped — monitor).
2. Minors #4–#7 (backlog, need go-ahead).
