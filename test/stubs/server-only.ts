// Test stub for the `server-only` package. In the real app that import is a
// guard that fails the build if a server module is pulled into a client bundle;
// under Vitest (plain Node) there is no such bundle, so it is a harmless no-op.
export {};
